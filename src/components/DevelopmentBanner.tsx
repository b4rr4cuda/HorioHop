/**
 * DevelopmentBanner.tsx
 * 
 * Development mode banner overlay (deprecated - now shown as dialog from header).
 * Kept for backwards compatibility but no longer used.
 */

export function DevelopmentBanner() {
  // Banner is now shown as a dialog from the header instead
  // This component is kept for backwards compatibility but returns null
  return null;
}

